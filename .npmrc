# npm config for LadeCompile monorepo
# No pnpm-specific keys — using npm workspaces.
# Workaround note: pnpm's isolated store uses junctions/symlinks that
# require Developer Mode / elevation on Windows, which breaks
# `next build` tracing for OpenNext. npm's flat node_modules avoids this.
